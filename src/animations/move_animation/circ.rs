// CPU-side easing curve helpers; GPU compute shader uses its own versions
// CPU-side easing curve helpers; GPU compute shader uses its own versions

#[allow(dead_code)]
pub fn to_dsl_string() -> String {
    "circ".to_string()
}

